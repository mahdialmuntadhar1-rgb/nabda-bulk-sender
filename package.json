{
  "name": "nabda-bulk-sender",
  "version": "1.0.0",
  "description": "Compliant bulk WhatsApp messaging CLI for Nabda Gateway",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "send": "node dist/cli.js send",
    "webhook": "node dist/cli.js webhook",
    "start": "npm run build && npm run send"
  },
  "keywords": ["whatsapp", "nabda", "bulk", "messaging"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "csv-parse": "^5.5.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/csv-parse": "^1.2.5",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
